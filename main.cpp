#include <iostream>
#include <glad/glad.h>
#include <GLFW/glfw3.h>

// #include "Engine.h"
// #include "logger.h"



int main() {
	return 0;
	//EngineConfig config;
	//printConfig(config);
	//Engine engine(config);
	//engine.start();
	//BOOST_LOG_TRIVIAL(info) << "The engine is dead";
	//return 0;
}
